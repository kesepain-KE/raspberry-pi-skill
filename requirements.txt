# Core dependency for Raspberry Pi 3/4 and legacy GPIO access
RPi.GPIO

# Optional high-level GPIO API for future device-layer features.
# gpiozero

# Optional WiringPi C API reference only; scripts do not depend on it.
# wiringpi
